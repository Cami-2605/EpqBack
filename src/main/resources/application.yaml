spring:
  application:
    name: epq-backend

  datasource:
    url: jdbc:mariadb://localhost:3306/epq_db
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect